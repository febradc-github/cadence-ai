---
description: Files a note you dictate into the knowledge vault. You author the content; the curator only files, tags, and links it. Works in both capture modes.
argument-hint: "[note]"
disable-model-invocation: true
---

Use the Skill tool to invoke the `turnstile-remember` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
