---
description: Stashes the in-progress ticket so urgent unrelated work can start: status parked, a resume note (current state, next step, blockers) written into the item note. /turnstile:pickup offers to un-park it later.
argument-hint: "[reason]"
disable-model-invocation: true
---

Use the Skill tool to invoke the `turnstile-park` skill, passing `$ARGUMENTS` and the rest of this message unchanged.
